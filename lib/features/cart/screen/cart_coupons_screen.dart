import 'package:flutter/material.dart';

class CartCouponsScreen extends StatelessWidget {
  const CartCouponsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Cart Coupons')),
      body: const Center(child: Text('Cart Coupons Screen')),
    );
  }
}
