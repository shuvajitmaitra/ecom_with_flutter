import 'package:flutter/material.dart';

class CategoryBrandsScreen extends StatelessWidget {
  const CategoryBrandsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Category Brands')),
      body: const Center(child: Text('Category Brands Screen')),
    );
  }
}
